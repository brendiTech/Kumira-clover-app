<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Clover Permissions -->
    <uses-permission android:name="com.clover.permission.ACCOUNT" />
    <uses-permission android:name="com.clover.permission.INVENTORY" />
    <uses-permission android:name="com.clover.permission.ORDERS" />
    <uses-permission android:name="com.clover.permission.PAYMENTS" />
    <uses-permission android:name="com.clover.permission.MERCHANT" />
    
    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@android:drawable/sym_def_app_icon"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CloverKiosk"
        android:name=".CloverKioskApplication"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CloverKiosk"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Clover App Market intent filter -->
            <intent-filter>
                <action android:name="clover.intent.action.APP_MARKET" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- Clover Account Provider -->
        <meta-data
            android:name="com.clover.intent.action.APP_TRACKING_ID"
            android:value="clover-kiosk-app" />
            
    </application>

</manifest>
